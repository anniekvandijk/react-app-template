import React, { Component } from 'react'

class Button extends Component {
    render() {
        return (
            <Button
                className="button"
                text="Button"
            />
        );
    }
}

export default Button;